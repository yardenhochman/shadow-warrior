import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.shadowwarrior.brain',
  appName: 'shadowwarrior',
  webDir: 'dist/capacitor',
};

export default config;
