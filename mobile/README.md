The webapp opens access to the access to the accelelrometer data, microphone data
This can run we web app without installation with pure js.
The output will be a BLE commands to a led matrix.
The song volume will play as a factor of the accelerometer energy and mic.

